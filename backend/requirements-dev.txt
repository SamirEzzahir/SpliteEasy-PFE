# Developer tools only — NOT installed in the production image.
#   pip install -r requirements-dev.txt
# Runtime dependencies live in requirements.txt.
-r requirements.txt

ruff==0.6.9
black==24.10.0
pytest==8.3.3
